import { apiInitializer } from "discourse/lib/api";

export default apiInitializer("1.8.0", (api) => {  
  // Glimmer topic list is the default in modern Discourse
  // This file can be removed or left as a no-op
});
